package edu.tpu.ruban.presentation.viewmodel

import edu.tpu.ruban.domain.entities.Product

class ScanViewModel(
    private val
) {

    fun getProductsMatches(barcode: String, barcodeType: Int, localizationId: Int) : List<Product> {

    }
}